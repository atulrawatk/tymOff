enum LoginType { SIMPLE, GOOGLE, FACEBOOK }

enum OtpVerificationFlow { login, signup, forgot_password, forgot_otp }

enum MediaType { text, image, video, article, gif }

enum UploadUIActive { IMAGE, VIDEO, RICH_TEXT, WEB_LINK, NONE }

enum DashboardActiveTab { HOME, DISCOVER, NOTIFICATION, PROFILE, LOGIN, NONE }

enum ProfileActiveTab {MY_POST, LIKE, MOST_LIKE, COMMENT}

enum SelectionUploadChipTypes { GENRE, LANGUAGE, }

enum ContentChangeType { ADD, UPDATE, DELETE, }
